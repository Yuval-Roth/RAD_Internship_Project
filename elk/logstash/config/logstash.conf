input {
  file {
    path => ["C:/Users/Yuval/git/RAD_Internship_Project/compiled_jars/logs/application.log"]
    start_position => "beginning"
    #sincedb_path => "NUL"
  }
}

output {
  elasticsearch {
    hosts => ["http://localhost:9200"]
    index => "logs"
  }
  stdout { codec => rubydebug }
}
